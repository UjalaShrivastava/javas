// for in-------------
const myObject = {
    js: 'javascript',
    cpp: 'C++',
    rb: 'ruby',
    swift: 'swift by apple'
}

for (const key in myObject) {
    // console.log(`${key} shortcut is for ${myObject[key]}`);
    
}


const fruits = ["Apple", "Orange", "Guava", "Litchi", "Grapes", "Banana"]
for (const key in fruits) {
    // console.log(key);
    // console.log(fruits[key]);
}